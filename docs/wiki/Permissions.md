# Permissions

CraftLedger Jobs registers Forge permission nodes:

- `craftledger_jobs.admin`
- `craftledger_jobs.balance.other`
- `craftledger_jobs.balance.top`
- `craftledger_jobs.transactions`

With Forge's default permission handler, admin-style commands fall back to Minecraft operator level `2`.
